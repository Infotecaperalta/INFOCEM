	=sys(1037)
	set console off
	set device to printer
	set printer on
USE f_clie 
REPORT FORM f_clie TO PRINT
	set printer off
    set printer to
	set device to screen
	set console on
close data
clear
do logo
RETURN
